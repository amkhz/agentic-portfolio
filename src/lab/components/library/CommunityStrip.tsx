// One-line adjacency note for the scholarly community the lab sits
// next to. No links today — the three names are intentionally flat
// so we can wire them to their own pages or external sites later
// without redesigning the strip.
export function CommunityStrip() {
  return (
    <p className="mt-6 font-lab-mono text-xs tracking-wide text-lab-text-muted">
      Adjacent to the work of Sol Foundation, The Visible College, and Hyperstition.
    </p>
  );
}
