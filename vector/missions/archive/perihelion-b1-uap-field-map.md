# Mission: B.1 format pass — uap-field-map

**Date:** 2026-06-10
**Branch:** `feat/perihelion-b1-uap-field-map` (off main, after PR #99)
**Lane:** Workstream B.1 per-guide pipeline, seventh guide and the first non-DIRD paper guide since the emergent-quantization/quantum-gravity pair. Spec: `plans/perihelion-format-alignment-brief.md` + `plans/perihelion-b1-renderer-scope.md`. Templates: `vector/missions/archive/perihelion-b1-dird-28-breakthrough-cockpits.md` (primary), plus the archived dird-15, dird-14, dird-13, emergent-quantization, and quantum-gravity logs.

## Scope

`uap-field-map.md` is the original proving guide for the callout pattern and arrived in the most format-modern condition of the series: 42 proper two-block callouts from the original authoring, emojis and heading markers already swept by PR #64, and a fully conforming frontmatter (slug, kicker, complete `source` with `url` and `arxiv`, `territory`, `order`, no empty `figures:` field). This is NOT a DIRD guide — the `source.authors` decision tree does not apply; the Knuth et al. attribution, 2025 year, venue, and arXiv ID were verified present and left standing. The sweep here was the glossary/marker audit, inline glosses, and the resolution of the empty Read Next placeholder outstanding since PR #56.

## The Read Next placeholder (deferred flag from PR #56)

**Call taken: removed the placeholder, added one proper Read Next elsewhere.** The empty `> **Read Next**` block sat after the Sturrock-receipt subsection of `#framing-scientific-problem` with no body paragraph. The format spec requires a Read Next to point to a related guide already in the library, by title; the renderer scope's defensive rule drops a bodyless callout to `plain`, so it rendered as a stray bold "Read Next" with no content. No library guide matches the Sturrock/AAS-survey context, so filling it in place would have manufactured a link — the brief's leave-it-out rule wins. Removed.

The dangling intent was realized where the prose actually names a library guide: a Read Next to **UAPx Field Methods** added in `#field-studies` (UAlbany-UAPx subsection, after the σ-threshold paragraph; the subsection opens by explicitly citing "the lab's existing UAPx Field Methods guide"). Body written in the sibling-guide prose style (no markdown links, no em-dashes), description grounded in that guide's own frontmatter. One of the two-per-guide Read Next slots used; the second is left open and flagged below.

## What changed

- **Glossary: 73 entries in, 73 out; 0 orphans; no exact duplicate keys.** But the audit found **18 unused entries** (no `|term|` marker anywhere in the body) — a profile no prior guide in the pipeline had. Five of the 18 are case/format near-duplicates of used keys with diverging definitions (`Residuum` vs `residuum`, `Angel hair` vs `angel hair`, `Phase Change Material (PCM)` vs `Phase Change Material`, `Science Traceability Matrix (STM)` vs `Science Traceability Matrix`, `Project SIGN / Grudge / Blue Book` vs `Project SIGN`) — the later lowercase/short entries (file lines 84–89) were clearly written to supersede the originals. Consolidation requires choosing which definitions survive, which is content work; flagged for Glossarian/B.2 rather than deleted here. Three unused entries were resolved by adding their first marker on a clean literal mention (below); **15 unused entries remain**, flagged.
- **8 new `|term|` markers**, no prose rewritten, all on exact-match literal text per the dird-15/28 precedent:
  - Five close true-first-mention gaps for already-marked keys: `|AATIP|` and `|AARO|` in `#framing` ("between the 2017 New York Times disclosure of |AATIP| and the 2022 standing-up of |AARO|" — both previously first-marked a section later), `|AAWSAP|` in the `#framing` Territory Bridge (that callout already carries |T4|/|T1|/|DIRD| markers; body marker at `#framing-scientific-problem` retained, both resolve), `|Sturrock|` in `#framing-scientific-problem` ("Peter |Sturrock| surveyed", previously first-marked two sections later), and `|Trans-medium travel|` in the `#observables` Five-Observables table (row 4; markers in table cells are parser-supported, per the existing `**|ADS-B|**` cell in the Galileo table).
  - Three are first markers resolving previously unused entries: `|FLIR|` in `#framing-scientific-problem` ("Three US Navy |FLIR| videos"), `|Hessdalen|` in `#history-compressed` ("the |Hessdalen| Project" in the Pocantico volume list — true first literal mention; the dedicated `#field-studies` subsection mentions it only in a heading and as "Hessdalen Valley"), and `|UAlbany-UAPx|` in `#field-studies` (inside the bold UFODAP sentence; mid-bold markers are explicitly parser-supported).
  - Totals: 67 → 75 resolved term nodes, 55 → 58 distinct marked terms.
- **3 inline definition glosses** (colon separator per renderer scope), only for marked terms the prose does not define in place: `BAASS` (`#framing-scientific-problem`, full three-sentence entry), `HR-ICPMS` (`#evidence`, after the Ubatuba paragraph, full two-sentence entry), `FTIR microspectroscopy` (`#evidence`, full two-sentence entry). All three sourced verbatim from frontmatter and already em-dash-free — unlike dird-13/14/15, no punctuation-only frontmatter restructures were needed. **Placement deviation:** the FTIR gloss sits after the Claymont composition bullet list rather than immediately after the introducing paragraph, because that paragraph ends in a colon that flows into the list; the post-list slot is the nearest legal position.
- **Callout retrofit: none needed.** 21 Design Hook, 17 Territory Bridge, 3 Subguide queued already in proper two-block form from the original authoring (this guide is the brief's "proves the pattern works" reference). No Design Implications closing section exists to convert. Net callout count stays 42: the empty Read Next removed, the real one added.
- **Frontmatter: no changes.** Already on `slug:`, kicker "Research Guide Series", complete source block, `territory: T4`, `order: 8`, no `figures:` field. `status: complete` left exactly as found per pipeline instruction (noting it differs from the dird sequence, where `status: draft` stands until B.2).

## Verification

- Validation ran exclusively via npm scripts per the locked workflow. Gloss and callout patterns conform to the renderer-scope detection rules by construction: single-paragraph colon-separated glosses, two-block exact-match bold labels with the blank `>` separator line.
- `npm run audit:orphans`: 0 orphans across 0 guides (audit at `vector/audits/orphan-terms-2026-06-10.md`, unchanged).
- Bounded grep sweep: 21 Design Hook, 17 Territory Bridge, 1 Read Next, 3 Subguide queued labels; 75 `|term|` markers across 58 distinct keys.
- Gates: 102/102 tests, lint 0 errors (1 pre-existing fast-refresh warning in `renderSection.tsx`), build green.

## Exit flags

- **15 unused glossary entries**, including the 5 near-duplicate pairs above. The duplicates need a consolidation call (which definition survives; the unused variants carry detail the used ones lack, e.g. Blue Book dates in `Project SIGN / Grudge / Blue Book`). The other 10 — `AARO predecessors`, `Florence 1954`, `Forensic vs. observational paradigm`, `Galileo Project`, `IFEX`, `JANAP 146 / AFR 200-2`, `Local Positioning System (LPS)`, `SETI Kingsland`, `VASCO`, `Wendt-Duvall sovereignty argument` — have no clean exact-match literal in body prose to mark: Galileo Project/IFEX/SETI Kingsland/VASCO appear only in headings and possessives, Wendt-Duvall only in a heading, and the rest never appear as their literal key string. Resolution needs prose touches or entry deletion: Glossarian/B.2 lane.
- **No `Hal Puthoff` entry** despite two body mentions, though dird-13 and dird-15 both carry one. The first clean mention ("with **Hal Puthoff** as Chief Scientist", `#framing-scientific-problem`) is bold; marking it would swap author-applied bold for a marker, a formatting change declined here. B.2 call.
- **Two dangling-colon content gaps**, likely conversion losses: `#field-studies` IFEX subsection ("The most significant single fact in this section:" — the fact never follows; the glossary's `IFEX` entry suggests it was the VaMEx3-MarsSymphony first-government-funded-system claim) and `#methodology` light-based imagery subsection ("Most striking specific datum:" — the datum never follows; the next paragraph discusses UAP appearing colder than ambient, so the lead stat is missing). Content work, not touched.
- **Second Read Next slot open** (cap is two per guide). Strongest candidate: DIRD 15, named in two Territory Bridges (`#field-studies` Hessdalen, `#methodology` Faraday polarimetry); those bridges already carry the connection semantically, so adding it was declined as over-callouting. Justin's call in B.2.
- **`INTERMAGNET` gloss declined**: the term is introduced only inside the Galileo instrumentation table, and there is no precedent for glossing table-introduced terms. Entry is clean two sentences if B.2 wants it.
- **Em-dash density in inherited copy:** 136 lines carry em-dashes from the original authoring, including most callout bodies and glossary definitions. New copy added in this pass is em-dash-free. B.2's lane.
- **Glossary entry length:** many of the 73 entries run 3 to 5 sentences against the 1 to 3 sentence rule (e.g. `Angel hair`, `Florence 1954`, `Pocantico Meeting`, `Project SIGN`, `residuum`, `SBIRS`, `Push-broom satellite imagery`, `UAP Tracker Project`). Trimming is content work; flagged for B.2.
- **`status: complete`** left exactly as found per instruction. If the orchestrator wants this guide to follow the dird sequence (draft until B.2 voice pass), the flip-down is its call, not this pass's.
- **B.1 library status:** with this pass, `uapx-field-methods.md` is the last guide without its per-guide B.1 sweep.
- **B.2 next:** the guide structure is canonical; the voice sweep inherits the flags above.
