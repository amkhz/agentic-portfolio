# Mission: B.1 format pass — uapx-field-methods

**Date:** 2026-06-10
**Branch:** `feat/perihelion-b1-uapx-field-methods` (off main, after PR #103)
**Lane:** Workstream B.1 per-guide pipeline, eighth and final guide. Spec: `plans/perihelion-format-alignment-brief.md` + `plans/perihelion-b1-renderer-scope.md`. Templates: `vector/missions/archive/perihelion-b1-uap-field-map.md` (primary, the other non-DIRD), plus the archived dird-15, dird-14, dird-28, dird-13, emergent-quantization, and quantum-gravity logs.

## Scope

`uapx-field-methods.md` is NOT a DIRD guide — the `source.authors` decision tree does not apply; the Szydagis, Knuth, Kugielsky, Levy attribution, 2025 year, and arXiv URL were verified present and left standing. Unlike uap-field-map, this guide never received the PR #64 modernization, so it arrived in the early-B.1 mechanical-lift profile — but a clean one: zero emojis (nothing to sweep), all 8 section headers already carrying `{#anchor}` IDs, a 37-entry glossary with 36 markers and zero orphans, zero callouts, zero inline glosses. It is also the library's only figure-bearing guide: a populated 5-entry `figures:` array, 5 matching `{figure:slug}` body references, assets present at `public/labs/figures/uapx-field-methods/`, and the parser/renderer path (`parse-guide.ts` figure collection, `GuideFigure.tsx`) fully wired. The `figures: []`-removal rule does not apply; the whole figure layer was verified healthy and left untouched per the brief's leave-figures-as-is rule.

## What changed

- **Glossary: 37 entries in, 37 out; 0 orphans; no exact duplicate keys.** One unused entry found: `Cash-Landrum` (hyphen) is a short near-duplicate of the used `Cash–Landrum` (en-dash, the key the body marker carries; the longer en-dash entry reads as a later Glossarian orphan resolution). No hyphen literal exists in the body to mark, and consolidation requires choosing which definition survives — content work, flagged for Glossarian/B.2 per the uap-field-map precedent. All other 36 entries are marker-referenced.
- **2 new `|term|` markers**, no prose rewritten, both on exact-match literal text closing true-first-mention gaps for already-marked keys, per the uap-field-map/dird-15 precedent: `|deposited energy|` in `#instrument-stack` ("an estimate of the |deposited energy|", Cosmic Watch paragraph; the prior sole marker sat in `#radiation` a section later) and `|coincidence|` in `#instrument-stack` ("which makes a |coincidence| between a camera detection and a radiation count", the true first literal mention; the prior sole marker sat at the definitional mention in `#statistical-framework`). All four markers resolve. Totals: 36 → 38 resolved term nodes, 36 distinct marked terms unchanged.
- **5 inline definition glosses** (colon separator per renderer scope), only for marked terms the prose does not define in place: `microbolometer arrays` (`#instrument-stack`, after the FLIR paragraph), `skew-Gaussian function` (`#c-tap`, after the classifier paragraph), `fall-streak hole` (`#dark-spot`, after the ten-hypotheses paragraph), `clutter reflectivity` (`#dark-spot`, after the NEXRAD paragraph, before the fig-8 figure reference), `accidental coincidence rate` (`#statistical-framework`, after the ambiguity/anomaly definitions paragraph). All five sourced verbatim from frontmatter and already em-dash-free — like uap-field-map and unlike dird-13/14/15, zero punctuation-only frontmatter restructures were needed. Five glosses against the siblings' three is need-based, not drift: this guide defines most of its 36 marked terms in prose (the expository register is unusually self-glossing), and these five were the genuine gaps left.
- **1 callout: Read Next ×1** in `#statistical-framework`, after the closing paragraph that explicitly cites "The Knuth review paper that catalogs the new science of UAP." Points to **The New Science of UAP** (`uap-field-map`), reciprocating that guide's PR #100 Read Next to this one, per the dird-14/dird-15 reciprocation precedent. Body written in the sibling-guide prose style (no markdown links, no em-dashes), grounded in uap-field-map's own frontmatter description. No other callout types added — see discretion calls.
- **Frontmatter: 1 addition.** `arxiv: "2312.00558v4"` added under `source.url` (portfolio-side optional field; value lifted mechanically from the existing `venue`/`url` strings). Everything else left standing: `slug:` already migrated, kicker "Research Guide Series", `territory: T4`, `order: 7`, populated `figures:` kept. `status: complete` left exactly as found per pipeline instruction (matching uap-field-map's profile, differing from the dird draft-until-B.2 sequence).
- **Emoji sweep: zero found, zero removed.** Heading-marker reconciliation: all 8 anchors already present, none touched.

## Discretion calls

- **Design Hook ×0.** No "Design Implications" closing section exists to convert — the closer is `#future`, integral prose. The six bold-led paragraphs in `#lessons-learned` are content structure (the paper's lessons), not design implications; left as paragraphs per the dird-14/dird-15 precedent for bold-led content paragraphs. Authoring new hook bodies from woven prose is content work; the brief's over-callouting rule wins. Candidates flagged below for B.2.
- **Territory Bridge ×0.** The prose never names another territory or draws an explicit cross-territory connection; the brief forbids manufacturing implicit ones. Flagged below.
- **Subguide queued ×0.** No referenced material is actually queued anywhere; uap-field-map's three existing queues (institutional history, Wendt–Duvall, Pasulka/Kripal) don't overlap this guide's content, so there is nothing to mirror and inventing a queue entry would be roadmap work.
- **Declined `|ambiguity|` marker** on "a camera ambiguity" in `#radiation`: it precedes the formal definition and reads as a casual single-sensor usage, while the glossary entry defines the formal ≥3σ multi-sensor term — a semantic mismatch the exact-match rule shouldn't paper over. Intermediate unmarked mentions of "coincidence" (`#radiation`, `#dark-spot`, `#statistical-framework` opener) likewise left unmarked: first mention plus definitional mention carry the markers, per precedent.
- **`fall-streak hole` gloss placement**: sits after the ten-hypotheses paragraph (its marker's paragraph), accepting that its second sentence pre-empts the elimination detail in the following paragraph; that is the nearest legal slot under the first-mention rule.
- **`source.venue` left as the arXiv listing string** (`arXiv:2312.00558v4 [astro-ph.IM]`), now redundant with `url` + the new `arxiv` field. Replacing it with a journal venue requires publication research; flagged rather than guessed.

## Verification

- Validation ran exclusively via the npm gates plus bounded greps per the locked workflow; no ad-hoc scripts. Gloss and callout patterns conform to the renderer-scope detection rules by construction: single-paragraph colon-separated glosses, two-block exact-match bold label with the blank `>` separator line.
- Bounded grep sweep: 38 `|term|` markers across 36 distinct keys (deposited energy and coincidence carry two each); 5 definition-gloss labels; 1 Read Next label; 0 Design Hook / Territory Bridge / Subguide queued labels; 0 emojis; em-dash line count 45 before and after (no new em-dashes introduced).
- `npm run audit:orphans`: 0 orphans across 0 guides (audit at `vector/audits/orphan-terms-2026-06-10.md`, unchanged).
- Gates: 102/102 tests, lint 0 errors (1 pre-existing fast-refresh warning in `renderSection.tsx`), build green.

## Exit flags

- **Em-dash density in inherited copy:** 45 lines carry em-dashes from the original authoring, including the `#dark-spot` section heading ("The Dark Spot — A Forensic Worked Example"). Heading text is anchor-stable content; if B.2 retitles it, the `{#dark-spot}` anchor must stay. New copy added in this pass is em-dash-free. B.2's lane.
- **Glossary register mismatch:** most of the 37 entries are telegraphic fragments rather than the siblings' full-sentence definitions ("UAPx term: ...", "'Naive' = assumes feature independence", "MIP. The regime where...", "SiPM. Solid-state photon detector..."), and the en-dash `Cash–Landrum` entry is one ~70-word sentence against the 1–3 concise-sentence rule. Rewriting is content work; B.2.
- **`Cash-Landrum` / `Cash–Landrum` near-duplicate:** the unused hyphen entry needs a consolidation call (the en-dash entry is longer and is the one the body uses). Glossarian/B.2 lane.
- **Design Hook candidates for B.2:** (1) the `#c-tap` "What makes C-TAP design-relevant" paragraph (human-in-the-loop pipeline architecture); (2) the `#future` opener ("design infrastructure waiting to be built... a blueprint with gaps"); (3) each of the six bold-led `#lessons-learned` paragraphs carries a design-against implication (data pipeline integration, real-time satellite overlay, analyzability over collection volume, clock sync as load-bearing infrastructure, LPS positioning, funding-model pressure). Hook bodies need authoring: content work.
- **Territory Bridge candidate:** multi-modal sensor fusion is T3's literal domain and the radiation-coincidence method is the bridge material, but the prose never draws it. Justin's call in B.2.
- **Second Read Next slot open** (cap is two per guide). No other library guide is referenced in this guide's prose; the strongest thematic pair (uap-field-map) already holds slot one. Likely stays open unless B.2 wants a cross-territory pick.
- **Subguide queued candidates:** Galileo Project, IFEX, and VASCO each get single-sentence treatments in `#forensic-problem` and carry full glossary entries; whether any becomes a guide is roadmap, not format.
- **`ambiguity` marker gap:** "a camera ambiguity" in `#radiation` precedes the formal definition (see discretion calls); a B.2 prose touch could rephrase it or accept the pre-definition marker.
- **`source.venue` redundancy:** venue duplicates the arXiv identity now carried by `url` + `arxiv`. If the paper has a journal home (the body and description suggest field-standard adoption circa 2025), venue should name it; research task, not guessed here.
- **Figure slugs carry `fig-N-` prefixes** (`fig-1-ufodap-deployment` vs the format example's semantic slugs). Cosmetic only; parser and renderer are indifferent. Noting in case a normalization pass ever wants it.
- **`status: complete`** left exactly as found per instruction, matching uap-field-map's profile rather than the dird draft-until-B.2 sequence; any flip is the orchestrator's call.
- **B.1 library status: complete.** With this pass, all eight guides have received their per-guide B.1 sweep. The B.2 voice lane owns everything flagged above.
