# Mission: The Flight Deck (Works 01)

> The single source of "where are we" for this build. Every work session reads this first; every PR that advances the build updates the **Now** line and checklist in the same PR (that is the definition-of-done rule that keeps nothing from slipping). Keep it lean: current state, locked decisions, open threads. History lives in git and the PRs.

**Now:** Shelf shape + PR BUILT 2026-07-05, in review (this PR): the Perihelion home gains the Works section per the resolved §10 direction. Shape ran docs-answered (brief: `vector/briefs/works-shelf-shape-2026-07-05.md`; probes skipped, Archive-side surface under imagery restraint), then built in the same PR. Landed: the **Flight Deck sigil** commissioned and drawn (`src/works/sigils/FlightDeckSigil.tsx`: the field ring seen as a gauge in the house sigil's hand — bone wall ring, dense ramp-top arc, three stress-bearing ticks, committed-bearing stroke, brass arrival point with halo; standalone leaf module with zero deck imports so the shelf imports it statically without breaking chunk isolation, `workSigils` registry keyed by slug as the Works 02+ template; ink rides `currentColor`, accent and emission ride `--sigil-accent`/`--sigil-halo` set per consumer, so no tokens leak in either direction); **WorksShelf** on the lab home after the territory shelves (kicker "The applied arm", h2 "Perihelion Works", one scaffold premise line awaiting the Writer pass, single bench entry: registration `W · 01 · 2026`, title + thesis line straight from the manifest, "Built from 4 Archive guides · In progress"; kin to the Accession Register through the hairline shelf rules and 7rem gutter but a drawn-mark-plus-title-block grammar, so it reads as an instrument among books, not a fifth territory; accent rides the untouched `--guide-accent` cascade whose fallback is already house brass; hover/focus = wash + title ink + the sigil's emission brightening); the **decline card gains the mark** (its standing "sigil lands with the Shelf work" note closed; emission speaks `--deck-caution` there). Justin's live note built same day in-PR: end-of-shelf placement stands, but the arm gets a **masthead notice** above the Field Notes (`WorksNotice`: small sigil, one sentence naming Perihelion Works + the Flight Deck, "Skip to the bench" jump link that smooth-scrolls to the anchored section in the guide-anchor idiom, reduced-motion honored, focus handed to the section). Gates: 352 tests (+4, WorksShelf suite), lint/build clean, deck chunk 72.1 of 350 KB gz, tone untouched at 81.1. Verified on the dev-preview server: section renders in both lab modes, hover/focus halo rules compiled, decline card and full deck boot with a clean console. Prior state (Phase 7 merged 2026-07-05, PR #187, with the live-pass rounds, control treatment B, and the closed boot-feel thread) lives in git history and `vector/audits/`. **Next per Justin's sequencing:** (1) **colophon copy** (Writer + Gaff, Joi-gated, D7 arm line; the shelf premise line rides that pass too, single constant in `WorksShelf.tsx`), then IMMEDIATELY (2) **launch Works 01.1** (field slice plane + locator ghost, `plans/works-01-field-slice-plane.md`) and (3) **investigate the redraft-exit feel** (the motion audit's ~160ms proposal-exit suggestion; commit-adjacent, decide with a live eye). Roy's standing flag unchanged: DeckSession's `useParadigmDissolve` (+ shutdown block) extraction is DUE the next session that touches the file. Dev tuning: `dialkit@1.3.0` drives the field shader (`src/works/flight-deck/dev/FieldTuner.tsx`); dev-only, absent from production.

## Reading order for a fresh session

1. This file.
2. `vector/decisions/ADR-017-perihelion-works-arm.md` — ratified architecture (route, tokens, motion lanes, deps, budget).
3. `vector/briefs/works-01-flight-deck-shape-2026-07-03.md` — confirmed interaction model and key states.
4. `plans/works-01-the-flight-deck.md` — the original plan (reference; superseded where the ADR or shape brief says otherwise).
5. `plans/dird-34-source-extraction.md` — evidence base for the three DIRD 34 instruments (findings ledger + final section).

## Locked decisions (do not relitigate)

- Name: **The Flight Deck**. Route `/w/flight-deck`, outside LabLayout, per-work tokens at `src/works/flight-deck/tokens.css` (ADR-017 D1/D2).
- Motion lanes: reactive = `motion` springs, authored = GSAP timelines (D4). Commit moment: tighten, substance-transfer handoff (never a crossfade), lagged consequence; one timeline owns both layers.
- Audio: alert grammar ships (3 severity signatures + confirmation tone, Tone.js behind the opt-in toggle); spatial soundscape is a fast-follow.
- Drill: guided ECAM checklist, one judgment step per alert; unfailable, no timers; false alarm is beat 2.
- Dissolve: staged regimes, scrubbed feel.
- Wallace is sanctioned for Works (concept art, mood frames, probes); imagery restraint is portfolio/Archive doctrine only.
- Colophon cites the dird-34 guide directly (`core/lab/guides/dird-34-cognitive-limits.md`, complete as of PR #169).
- Home linking: evolve the Shelf (Works section, instrument-on-the-shelf register); foyer deferred to Works 02.
- Boot ritual: **D+A blend, locked 2026-07-03.** Hold-to-start gesture, certification self-test grammar per instrument (sweep, lamp flash in severity order, caption, then data), completion lands as a one-breath deck settle (synchronized gauge overshoot, bench edge to operator channel) plus a subtle emission on ready light and gauges (bloom once, hold a quiet glow: an awake deck gives off its own light; ready light emits in the operator channel, gauges in their own bone light so telemetry and watcher stay unconfusable). One GSAP timeline; the hold scrubs the playhead, release runs it backward. Machine events `WAKE`/`ABORT_WAKE`/`BOOT_COMPLETE` already fit. Choreography reference: the riff-sheet artifact (2026-07-03 session). Doctrine note, per Justin same day: slop bans are overridable anywhere when the move has meaningful use, not just for show. Execution amendments from live review of the built ritual (2026-07-03, PR #175): the settle's positional overshoot read as a bounce and was cut; the one-breath settle is carried by a single brightness exhale plus the emission bloom traveling bench edge to operator channel. Hold-to-start gained visible charge feedback: progress ring strokes in with the playhead, lamp glow swells under the finger, both drain on early release.

## Build checklist

Each phase is a shippable PR. Tick items here in the same PR that lands them.

- [x] **Wallace direction probes** (before phase 2): instrument-bench material study; field-render spectral-ramp study; consciousness-chamber end state. Baked-text call per render with Justin. Record in `mocks/flight-deck-probes/` (6 renders + caption sidecars, 2026-07-03; bench + ramp re-rendered same day under strict typography-craft captions, strict validation on). Outcomes: bench register validated (bench s11 is the direction reference; s47's physical needles noted as retro drift, not the lane); readings-are-HTML confirmed (incidental baked micro-text garbles, deliberate mono strings render perfectly, 12/12 across both passes); chamber s47 is the movement-5 end-state reference. Ramp finding: Ideogram's perfusion prior resists a fully red-free ramp (two palette passes, red still bleeds in below the amber top), so the probes stand as register references, not color specs; the amber-top ramp is enforced exactly by the WebGL shader in phase 2.
- [x] **Phase 1 — Skeleton:** route + lazy chunk, scoped tokens, `core/works/` manifest, state machine, colophon chrome, mobile decline card, reduced-motion/no-WebGL static plate scaffold.
- [x] **Phase 2 — Field Integrity** (hero instrument, ogl shaders; built 2026-07-03). Shader reads the five ramp stops from tokens.css and interpolates in OKLab (violet-to-amber desaturates through warm neutral, never red); boot ritual is one GSAP timeline per the locked score; static plate got a no-WebGL CSS ring from the same tokens. → **Roy reviewed 2026-07-03**: HOLD (ink-faint on small text vs AA) cured in-PR via `--deck-ink-label`; mask literal tokenized; shader/readings clock unified. Report: `vector/audits/roy-review-2026-07-03-works-01-phase-2.md`.
- [x] **Annotation layer ("earn the width")** (merged 2026-07-04, PR #180 + #179 stack repair): caliper arcs + leader lines from the three tracked stress concentrations to per-lane readouts (`ST n` / bearing / intensity / watch state), projected in `fieldAnnotation.ts` by inverting the shader transform (frame dials, aspect guard, ring centerline) so annotation and render share one geometry; lane text ticks on the readings cadence from the same sample; per-lane sr mirror sentences; aspect-driven presence, no timed entrance. Justin's live gut-check passed same day.
- [x] **Phase 3 — Synthetic Orientation + Vacuum Energy** (built 2026-07-04): two pure models join the field's pattern (`core/works/flight-deck/orientation.ts`, `vacuum.ts`: deterministic, nominal drift only, the drill will feed the same shapes adversarial inputs). Synthetic Orientation = slim WebGL horizon strip in the horizon region (banking hairline + faint flow streaks hugging it, ladder lines fixed in the bench frame as the still reference; bone ink only, felt before looked at; spatial audio stays parked per the shape brief). Vacuum Energy = the invented lattice gauge (SVG, no render loop, re-samples on the readings cadence: extraction accumulates hairlines, demand is the one firm line, margin is the gap). Gauge form reworked same day after Justin's first look (four-variation board): the wide lattice bed spanning the region width, plus the expanded margin strip (demand-centered ±0.06 window, ×8; real-instrument precedent: expanded ILS/radar-altimeter scales) — the strip is one commit to revert if the live read says bed-only. Both instruments ride the generic boot sweep; static plates at t=0 + sr sentence mirrors per the a11y contract. Orientation strip approved live ("strip weight is good, animation looks good"); gauge combo (bed + expanded margin strip) approved live 2026-07-04 ("the combo works").
- [x] **Phase 4 — Control panel** (built 2026-07-04): the translation layer is live in the panel region. Intent fields (destination / timeline / risk, keyboard-native radios) drive `core/works/flight-deck/translation.ts`, which drafts three proposals per intent (direct / sweep / drift, deterministic, one-sentence summaries + route traces + DRAW/UTIL costs); the utilization meter tracks a pure activity model with the A2 band annotated, the 0.70 ceiling marked, needle warming + OVER text past it, and drafting cadence visibly slowing over the ceiling (DIRD 34 findings 2/4/8). Commit runs the locked choreography as one GSAP timeline: tighten, substance-transfer handoff (the route trace is consumed on the same value that raises the field's arrival glow), then the trim lands and the lag lives inside the shared envelope in `core/works/flight-deck/commit.ts` — from which Field Integrity blooms and leans at the committed bearing, Synthetic Orientation banks into the turn with flow rising, Vacuum Energy's demand steps with the margin pinching then recovering, and the meter blips. Justin's control-the-ship requirement wired first-class: one deck clock, one trim, every instrument answers. Panel plate for the static fallback; sr mirrors throughout; panel copy scaffold-grade pending the Writer + Gaff pass. Note: commits do NOT yet dispatch `COMMIT_SUCCEEDED` — the machine transition is tested and waiting, but firing it now would strand the session in a drill phase with no drill UI; phase 5 adds the dispatch when the drill can answer it. **Layout reworked same day after Justin's first look** (the tall right panel forced scroll and hollowed the center; option D picked from a four-option board): the right column is now a slim dock (intent, status line, vacuum gauge, meter at the base) and the drafted routes surface as a **transient review row in the center space beside the field render** (the shape brief's "reviewing" state, literally) — a commit consumes the row on the same timeline and the space returns to quiet, reserved for the drill's attention in phase 5. The bench is exactly one viewport tall (`100dvh`, verified no-scroll at 1600×900); the review band shares its row with the vacuum gauge so the bench never reflows mid-commit. Translation state lives in `useTranslationLayer` (shared by dock + row); the dock's status line carries the standing order after a commit ("En route: …"). Justin's live gut-check passed 2026-07-04 ("commit timing feels right"); the tighten 400 / handoff 500 / lag 600ms score stands as built.
- [x] **Phase 5 — The drill** (built 2026-07-05): all six beats live, worked at the operator's own pace, unfailable, no timers. The pure script, progress reducer, cadence score, and disturbance envelopes live in `core/works/flight-deck/drill.ts` (machine idiom: illegal actions are no-op same-reference); the aural grammar's signatures are pure data in `alertGrammar.ts`. `DeckSession.runCommit` now dispatches `COMMIT_SUCCEEDED` when the commit consumes the review row (the machine's once-per-session guard makes later commits no-ops), a quiet en-route beat (4.2s) passes, then the drill takes the center review space: guided ECAM checklist beside the render (`DrillProcedure`), alert region at master caution position (`AlertRegion`, fixed single-line height so a posting never reflows the bench). System steps tick themselves (1.6s); the one judgment step per alert waits forever and takes focus. Beat 2's verify shows lane B's fabricated claim beside the render's live wall (same pure model, same clock); "Re-check lane B" is a holding choice that answers and stays; dismissal carries the 20-25% line. Adversarial inputs ride the models' documented contract (`sampleX(t, trim, disturb)` additive deltas, deterministic in (t, timeline)): the caution drops evenness out of band and leans the horizon uncommanded 2.4° (mirrors now speak honest status words: "off nominal", "margin pinched", "demand over extraction"); the warning thins the wall to ~0.89, pegs the caution's own stress lane at the ramp top, and surges vacuum demand so the margin pegs the expanded strip (≤ −0.06 transient, proven across all drift phases in tests) then holds pinched below `MARGIN_FLOOR` until the redistribution judgment (even ring / shed aft) lands. Recovery is the reverse boot echo (one brightness exhale, emission wave operator channel back up the bench, `drillScore.settle`) with disturbance decays staggered vacuum → orientation → field; the residual (`DrillResidual`) reads the operator's actual calls back, shows the live recovering margin from the same model, ends warm, and "Return to watch" leaves the quiet trace "No active alerts · drill worked". Aural grammar ships: `tone@15.1.22` added, dynamically imported ONLY inside the sound toggle's gesture (operator strip chrome, `aria-pressed`, default off) — its chunk is named `tone` in the build (81 KB gz ≤ the 160 KB budget), zero static references, portfolio/labs entry hashes unchanged; three severity signatures + confirmation tone play once per posting, never loop, and everything audible is written (severity words, step ticks, one shared announcer live region so nothing double-speaks). Verified live at 1600×900: full drill worked end to end, one-viewport no-scroll held through every beat, drill did not re-arm on a second commit, Tone loaded clean. Deck chunk 64.9 of 350 KB gz; 314 tests (+41). → **Roy reviewed 2026-07-05: SHIP WITH NOTES**; both defensive nits (judge() re-resolution guard, residual trim note) cured in-PR; accretion flags (DeckSession timeline extraction, drill.ts script/envelope split) deferred to phase 6; focus-steal feel + polite-vs-assertive warning + 44px chrome targets ride Justin's live pass and the phase 7 audit. Report: `vector/audits/roy-review-2026-07-05-works-01-phase-5.md`. **Justin's live-pass feedback built same day, in-PR:** (1) resolved beats now dwell for their reading time (`betweenBeatsDwellMs`: 2.2s base + 240ms/word, cap 12s), so the false-alarm caption holds ~12s instead of flashing in a fixed gap; (2) the alert region rebuilt as a true annunciator (picked A+B+C+D from the strategy board): a posted alert speaks at instrument scale (0.9375rem/500) inside a row reserved at the posted size (1.625rem, verified zero bench movement), its lamp holds a severity-colored emission in the boot's `--emit` grammar (`--deck-emit-advisory/caution/warning` tokens) released on resolution, each posting plays a one-breath master-caution exhale (bench brightness dips 0.955 and returns) with the alert blooming in on one authored GSAP timeline (never loops; posting choreography moved off the entrance spring to the authored lane, which is where a choreographed event belonged), and the affected instrument's existing cert lamp holds lit in the severity color as the local echo; (3) the field render now shows what the drill's telemetry says (Justin: "if the wall is thin could we visualize that"): an out-of-band deficit term in the shader — zero anywhere above the same 0.94 band floor the readings speak, engaged only when the drill thins the wall — gaunts the shell, chills the density toward the ramp's cool bottom, and necks the wall hardest where a stress concentration sits (the danger picture: a bright concentration on a thinning wall). Centerline untouched, so the annotation projection stays glued; the three gains (`thinGain`/`neckDepth`/`thinCool`) ride the DialKit tuner as a new "deficit" group for live feel-tuning, defaults 0.38/0.55/0.08. Verified live at the warning plateau (WALL 0.89): visibly gaunt cold ring with a deep neck at the pegged lane's bearing.
- [x] **Phase 6 — Paradigm slider** (built 2026-07-05): the dissolve is pure first (`core/works/flight-deck/paradigm.ts`: continuous envelopes for light/hero/telemetry/panel/promotion/coupling, monotonic along the drag, panel gone by 0.5, coupling consciousness-only; `paradigmScore` for the authored crossings) and the watcher is a model (`operator.ts`: blink/respiration/coherence + the breath waveform itself, nominal human ranges under test; drill loading via `drillOperatorDelta` in the new `drillEnvelopes.ts`). The slider is staged-regimes-scrubbed-feel exactly per the shape brief: spring thumb (motion lane, critically damped), dissolve rides the spring value not the raw input, crossings detected on the thumb so authored transitions play while the drag continues (GSAP lane, `timelines/paradigmTimelines.ts`); regime names + one-line captions swap on the reactive lane; revealed once per session after `drillArmed` clears, with an emission bloom and an announcement. Consciousness end: chamber takes the center review space (arrival = strip hands its emission to center, bloom, promotion caption lands last at reading pace; departure reverses quick), deck rests near-dark (`--deck-bg-deep` floor, never black), and the field's ring visibly breathes on the operator's cadence with density leaning on coherence (coupling uniforms read the same pure model on the same deck clock as the strip and chamber, so nothing can disagree). OperatorStrip replaces the boot-era stub in the operator channel (SVG polylines, readings cadence, no render loop; t=0 still doubles as the plate's strip); sr mirrors throughout (strip + chamber speak the promoted state; regime crossings announce through the session's single announcer). Roy pre-reads done in the same PR: four authored timelines extracted from DeckSession into `src/works/flight-deck/timelines/` (behavior-identical) and drill.ts split into script (drill.ts) + envelopes (drillEnvelopes.ts). Deck chunk 69.3 KB gz; 342 tests (+28). In-PR follow-ups from Justin's first look (2026-07-05), round two: (1) the chamber's breath waveform and now ALL the strip's traces draw on gated rAF loops (the 400ms cadence stepped visibly on a ~4.6s breath; values + sr mirrors stay on the cadence); (2) the utilization explainer returned to the shape brief's hover reveal (title + sr) — the visible paragraph was overflowing onto the vacuum region once the slider claimed its operator-channel height; the panel region gained `overflow: clip` as the never-overlap guarantee; (3) the coupling proves itself ambiently ("visualize the coupling without clicking the controls"): commits now cost the watcher attention (`commitOperatorDelta` riding the consequence envelope, so the strip stirs whenever the ship moves), and the chamber gained the FIELD echo — a bone-ink second trace of the same waveform the bubble's wall is breathing, sampled `COUPLING_LAG_S` (= the commit score's own 0.6s lag) behind the operator's line; the field shader reads the operator through the identical lagged function, so the echo IS the ship's response and the intent-to-action gap is visible between the two lines. `operatorLoadAt` (drill + commit) is now the single door to the watcher's load for strip, chamber, and shader. → **Roy reviewed 2026-07-05: SHIP WITH NOTES**; the chamber-lifecycle race (stale departure onComplete could strand consciousness chamberless and leave the room half-faded) cured in-PR (departure timeline killed on re-arrival, arrival resets the chamber root) plus the echo-lag label now derives from `COUPLING_LAG_S`; accessibility notes (slider hit area, short-viewport clip focus, announcement cadence) queued to phase 7 with the phase-5 chrome-target item. Report: `vector/audits/roy-review-2026-07-05-works-01-phase-6.md`. **Justin's live pass 2026-07-05: signed off "it all works"; MERGED.**
- [x] **Phase 7 — Polish** (built 2026-07-05): the full charter ran — `/audit` (report `vector/audits/audit-2026-07-05-works-01-phase-7.md`), `/design-motion-principles` audit (PASSES wave-driven doctrine; branded report `vector/audits/motion-audit-2026-07-05-works-01.html`), `/interface-craft` review (sound; races cured), `/polish`. Roy's queued a11y notes all cured (44px targets, clip-focus escape, announce debounce); movement-5 plate still; dormant-name contrast fix (the one axe failure; deck a11y 100); single-announcer contract enforced; judgment focus handoff; arrival-race mirror cured; thumb + sweep on transform; caption exit score-true. D6 verified: deck 70.7/350 KB gz, tone 81.1/160, labs entry byte-identical, DPR ≤ 2, all rAF loops double-gated. Lighthouse: deck 97/100 (bars 70/95), Archive 97/100 (bar 90). Verified live boot-to-consciousness. Live-pass round one added Justin's four asks same day (lamp flash freed + lit, vacuum gauge on rAF, Shut down control with full-sequence restart, first-watch brief on the dormant overlay); round two smoothed the utilization needle onto the gated loop and added the brief's third line; control affordance resolved as treatment B (`--deck-control` brass rest on everything pressable, lifting to full amber under the hand); flash brightness resolved as built post-merge, closing the boot-feel thread; redraft-exit feel decision still queued. Full detail in the audit report, Roy's report, and PR #187.
- [x] **Shelf shape + PR** (built 2026-07-05, this PR): Works section on the lab home per the §10 direction (instrument on the shelf, not another book spine). Shape brief `vector/briefs/works-shelf-shape-2026-07-05.md`; `WorksShelf` after the territory shelves, manifest-driven; the Flight Deck sigil commissioned (`src/works/sigils/`, leaf-module + registry pattern for Works 02+) and landed on the shelf and the decline card. Thesis line was already in the manifest; section premise line is scaffold pending the Writer pass. Discoverability per Justin's live note: `WorksNotice` above the Field Notes introduces the arm and jumps to the section.
- [ ] **Colophon copy:** Writer + Gaff pass, Joi-gated; sources incl. dird-34 guide direct link; Works arm line (D7 candidate); retune the WorksShelf premise line in the same pass (single constant in `src/lab/components/library/WorksShelf.tsx`).

## Open build questions (resolve during build, record here)

- Display face (font procedure; voice words: calibrated, patient, luminous).
- Field-render spectral ramp: **resolved 2026-07-03; contrast formally closed 2026-07-05.** Amber top locked from the Wallace probe; phase 2 landed five stops as tokens (`--deck-field-stop-0..4`), OKLab-interpolated in-shader, Justin validated live. Phase 7 `/audit` ran the OKLCH→WCAG math: every text token clears AA with margin; ramp stops 0-1 sit under 3:1 by design (thin wall fading toward the room) and are 1.4.11-exempt because the wall state is always redundant in text. Numbers in the audit report.
- Hero composition at wide widths: **resolved 2026-07-04, Justin locked "earn the width"** (option B of the 2026-07-03 comparison artifact); **built same day** (see checklist). The anchor is the baked frame dials; the tuner still drives it live for the gut-check.
- Annotation lane legibility: **resolved 2026-07-05 with the drill's alert grammar, both halves of the parked riff built.** Columns speak plain words (`BEARING 034`; intensity carries a trend word instead of a column label: `0.62 · rising`), the trend is the model's real derivative (same pure model sampled 1.6s earlier, `formatStressLanes(field, earlier)`), and the alert/procedure copy uses the same plain register so lane vocabulary and alert vocabulary land as one language. Ships in this PR for the live gut-check.
- Drill re-arm: **built as once per session** (machine `drillArmed`, in-memory, so a page reload re-arms naturally; no persistence added). Relitigate live if repeat visitors deserve a fresh drill without reloading.
- Decline-card capture format (static frame vs short clip, weight budget).

## Fast-follow parking lot

**Works 01.1 — Field slice plane + locator ghost** (SCHEDULED: launches right after the Shelf + colophon items, per Justin at phase-7 merge; plan: `plans/works-01-field-slice-plane.md`; the ultrasound sweep answer to Justin's 3D/4D riff; Synthetic Orientation stays flat by the same plan's decision note — its 3D upgrade is the soundscape); spatial soundscape (Panner3D channel); tablet-landscape reduced deck (only if cheap); foyer question at Works 02; "tokenize lockup metrics when Works renders it a second time" (Director item, triggers when the colophon reuses the masthead lockup).

## Rules of engagement (every session)

Feature branch, never main. `npm run lint` + `npm run build` before done. OKLCH tokens by name only. No em-dashes in copy. Update this doc in the same PR as the work. State files touched and their layers.
